
  # Touchscreen Kiosk UI

  This is a code bundle for Touchscreen Kiosk UI. The original project is available at https://www.figma.com/design/YSunjbL76fmAPlfCWjdUKW/Touchscreen-Kiosk-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  